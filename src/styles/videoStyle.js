import { StyleSheet } from 'react-native';

export const videoStyle = StyleSheet.create({
  videoPlayer : {
    zIndex : 99,
    justifyContent: 'center',
    alignItems: 'center',
    flexDirection: 'row'
  }
});